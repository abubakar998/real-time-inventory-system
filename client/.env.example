# Leave blank for local dev — Vite proxies /api and /socket.io to the server
# (see vite.config.js), so the app runs same-origin.
#
# Set this only when the client is deployed to a different host than the API,
# e.g. VITE_API_URL=https://drops-api.onrender.com
VITE_API_URL=
